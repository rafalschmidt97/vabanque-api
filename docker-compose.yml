version: '3.1'

services:

  db:
    container_name: "vabanque-postgres"
    image: "postgres:11"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "zaq1@WSX"

  adminer:
    container_name: "vabanque-adminer"
    image: "adminer"
    ports:
      - "5433:8080"
